# ADR-001: Claude 생산성 대시보드 — 1주 PoC 설계서

**상태:** 제안(Proposed)
**날짜:** 2026-05-20
**의사결정자:** 개발팀 리더/매니저(1차), 엔지니어링 헤드(최종)
**작성자:** 크리스 (chris.kim@doverunner.com)

---

## 한눈에 보기

이 문서는 **"Claude를 쓰면서 우리 팀이 정말 더 빨라졌는가?"** 라는 질문에 데이터로 답하기 위한 대시보드를 어떻게 만들지를 정리한 설계서입니다. 1주일 안에 동작하는 시제품(PoC)을 만들어보고, 효과가 보이면 본격 확대하는 것이 목표입니다.

핵심 결정 세 가지:

1. **AI를 썼는지 표시하는 방법** → 커밋 메시지 맨 아래에 `Assisted-By: claude` 한 줄을 자동으로 붙입니다.
2. **데이터 수집 방식** → 1시간마다 Jira와 Git에서 새 데이터만 가져와 작은 DB에 쌓습니다.
3. **대시보드 도구** → Python의 **Streamlit**으로 만듭니다. 차트 한 페이지를 5~10줄짜리 함수로 그릴 수 있어 1주일 일정에 가장 빠릅니다.

---

## 1. 왜 이게 필요한가? (Context)

요즘 우리 팀의 많은 개발자가 Claude를 씁니다. 그런데 정작 "도움이 됐다"는 건 대부분 **느낌**입니다.

> "이번 주는 좀 빨라진 것 같아요."
> "버그 잡을 때는 진짜 좋더라."

이 정도 수준의 정보로는 다음과 같은 결정을 내리기 어렵습니다.

- Claude 라이선스를 더 살까, 줄일까?
- 어떤 업무(신기능 / 버그 / 리팩토링)에서 효과가 큰가?
- AI를 쓰면 코드 품질이 떨어지지는 않나? (속도 ↑, 버그 ↑ 이면 손해)

그래서 매니저가 매일 아침 한 번 켜보면 **"우리 팀이 AI를 얼마나 쓰고 있고, 쓰면서 결과가 어떻게 변하고 있는지"** 를 한눈에 볼 수 있는 대시보드를 만들려고 합니다.

### 우리에게 주어진 조건

| 항목 | 내용 |
|---|---|
| 누가 봅니까 | 개발팀 리더/매니저 |
| 팀 크기 | 10~50명 |
| 데이터 최신성 | 1시간 주기로 갱신되면 충분 |
| 만드는 기간 | **1주일 안에 PoC** |
| 기술 스택 | Python으로 직접 만든다 |
| 데이터 출처 | Jira(이슈) + Git(커밋·PR) |

### 가장 신경 써야 할 세 가지 함정

1. **개발자에게 부담을 주면 안 됨** — "AI 썼어요" 표시를 매번 손으로 적게 하면, 사람들이 잊어버립니다. 잊으면 데이터가 비고, 대시보드는 거짓말을 합니다. → **자동화**가 필수.
2. **1주일이라는 짧은 일정** — 거창한 데이터 웨어하우스는 만들 시간이 없습니다. → **작고 단순한 구조**가 답.
3. **사람을 줄세우는 도구가 되면 안 됨** — "누가 AI 많이 썼나" 같은 개인 비교는 위험합니다. 측정 자체에 대한 거부감으로 이어집니다. → 1차 PoC에서는 **팀/저장소 단위 집계만** 보여줍니다.

---

## 2. 결정 사항 요약 (Decision)

```
   [개발자 컴퓨터]            [수집 서버]              [대시보드]
   ┌───────────┐         ┌────────────────┐         ┌───────────┐
   │ git commit│ ──────► │ 1시간마다       │ ──────► │ Streamlit │
   │  + 자동   │         │ Jira / Git에서  │         │ 차트 6개  │
   │  trailer  │         │ 새 데이터 가져옴│         │ (팀 단위) │
   └───────────┘         │  → SQLite       │         └───────────┘
                         └────────────────┘
```

- **AI 표시 방법:** Git 표준 기능인 **trailer**(커밋 메시지 끝의 `키: 값` 줄)를 활용합니다. Git 훅이 자동으로 `Assisted-By: claude`를 붙여줍니다. 개발자가 의식하지 않아도 표시됩니다.
- **저장소:** PoC는 **SQLite 한 파일**로 시작합니다. 팀이 50명을 넘으면 그때 PostgreSQL로 옮겨도 됩니다.
- **수집 방법:** Python 스크립트가 1시간마다 `cron`/`APScheduler`로 실행되어 새 데이터만 가져옵니다.
- **대시보드 도구:** **Streamlit**. Python 한 언어로 차트와 웹 페이지를 모두 만들 수 있어, 매니저 5명에게 보여주고 의견을 받아 즉시 고치기 좋습니다.

---

## 3. 비교해본 선택지들 (Options Considered)

### 3.1 "AI 사용 여부"를 어떻게 표시할까?

#### 선택지 A — Git Trailer 방식 ✅ 권장

커밋 메시지 마지막에 한 줄을 추가합니다.

```
feat: 청구서 CSV 내보내기 기능 추가

월별 청구서를 CSV로 받을 수 있는 버튼을 만들었습니다.

Assisted-By: claude
Refs: BILL-142
```

| 기준 | 평가 |
|---|---|
| 구현 난이도 | 낮음 (작은 Git 훅 하나) |
| 기존 컨벤션과 충돌 | 없음 |
| 파싱(데이터로 읽기) | 쉬움 |
| 확장성 | 좋음 ("강하게 도움받음/살짝 도움받음" 같은 정보도 추가 가능) |

**장점**
- `Co-authored-by:`, `Signed-off-by:`처럼 GitHub/GitLab이 이미 잘 다루는 표준 형식입니다.
- 제목을 건드리지 않으므로 릴리스 노트 자동 생성 도구와 충돌이 없습니다.

**단점**
- 개발자가 손으로 적으면 잊을 수 있음 → 자동 삽입 훅으로 해결합니다.

#### 선택지 B — 제목에 태그 붙이기 (`[AI] 청구서 CSV…`)

| 기준 | 평가 |
|---|---|
| 가시성 | 높음 (한눈에 보임) |
| Conventional Commits와 충돌 | **있음** |
| 정보 확장 | 어려움 |

제목에 `[AI]`를 붙이면 채팅창에서 바로 눈에 띄지만, `feat(billing):` 같은 기존 규칙을 쓰는 팀은 형식이 깨집니다. 또한 "강하게 vs 살짝" 같은 정보를 더 담기 어렵습니다.

#### 선택지 C — Conventional Commits scope 늘리기 (`feat(ai/billing): …`)

`commitlint`, `semantic-release` 같은 도구가 scope를 하나로 가정하기 때문에 깨질 위험이 큽니다. 1주 PoC 안에 부작용을 다 확인하기 어렵습니다.

### 3.2 데이터를 어떻게 모을까?

| 방식 | 어떤 식 | 1주 PoC 적합도 |
|---|---|---|
| **Pull (1시간마다 가져오기)** ✅ | Python 스크립트가 정해진 시간에 Jira/Git API 호출 | **좋음** — 단순, 인프라 1개 |
| Push (Webhook으로 즉시 받기) | Jira/Git이 새 이벤트를 우리 서버로 쏴줌 | 나쁨 — 외부에서 접근 가능한 서버, 큐, 재시도 로직이 다 필요 |
| 외부 제품(LinearB 등) 사용 | SaaS 결제하고 연동 | 나쁨 — 비용·구매 절차로 1주 안에 불가 |

매니저가 보는 대시보드는 본질적으로 **트렌드 관찰**이라, 1시간 지연은 문제가 안 됩니다.

### 3.3 어디에 저장할까?

| 옵션 | 평가 |
|---|---|
| **SQLite** ✅ (PoC) | 파일 하나, 설치 불필요. 수십만 행까지 잘 견딤. |
| PostgreSQL | 팀이 50명 넘어가면 자연스럽게 이행. PoC에는 과함. |
| BigQuery 등 클라우드 DW | 비용·셋업 시간으로 PoC 부적합. |

### 3.4 대시보드는 무엇으로 만들까?

| 옵션 | 평가 |
|---|---|
| **Streamlit** ✅ | Python만으로 완성. 차트 한 페이지 = 함수 하나. 1주 PoC에 최적. |
| Dash (Plotly) | Streamlit보다 강력하지만 코드가 길어짐. PoC 다음 단계에 적합. |
| Grafana | 시계열 모니터링엔 좋지만 비즈니스 지표/필터 UX가 약함. |

---

## 4. 핵심 트레이드오프 (Trade-off Analysis)

크게 두 개의 줄다리기가 있습니다.

**첫째, 가시성 vs 도구 호환성.** 제목에 `[AI]`를 붙이면 즉시 눈에 띄지만, 기존 자동화 도구와 부딪힙니다. Trailer는 평소엔 잘 안 보이지만 표준 형식이라 안정적입니다. **"눈에 띄는 것"보다 "데이터가 정확한 것"** 이 대시보드의 본질이므로 trailer를 택했습니다.

**둘째, 신선도 vs 만드는 비용.** Webhook은 거의 실시간이지만, 만들고 안정화하는 데 1주를 다 씁니다. Pull 방식은 1시간 지연이 있지만 매니저용 트렌드에는 충분하며 일정에 맞습니다.

---

## 5. 어떻게 동작하나? (System Architecture)

```
   [Jira REST API]          [Git provider API]
        │                          │
        │  최근에 변경된 것만       │  최근 커밋·PR만
        ▼                          ▼
   ┌──────────────────────────────────────────┐
   │   etl.py  (1시간마다 자동 실행)           │
   │   - Jira 이슈/상태변경 가져오기            │
   │   - Git 커밋·PR 가져오기                   │
   │   - 커밋에서 Assisted-By: 줄 찾아         │
   │     ai_flag 컬럼에 True/False 저장        │
   └──────────────────────────────────────────┘
                       │
                       ▼
                ┌─────────────┐
                │ data.sqlite │   ← 한 파일에 다 들어감
                └─────────────┘
                       │
                       ▼
   ┌──────────────────────────────────────────┐
   │  dashboard.py  (Streamlit 앱)             │
   │  - 팀 / 기간 필터                          │
   │  - 차트 6개 (아래 6번 참고)                │
   └──────────────────────────────────────────┘
```

### 테이블 구조 (단순 버전)

| 테이블 | 컬럼 (핵심) |
|---|---|
| `commits` | sha, repo, author, committed_at, additions, deletions, **ai_flag**, ai_level, jira_keys |
| `issues` | key, project, type, status, created_at, resolved_at, story_points |
| `issue_transitions` | issue_key, from_status, to_status, at |
| `pull_requests` | repo, number, opened_at, merged_at, additions, deletions, **ai_flag** |

`ai_flag`는 커밋 메시지에 `Assisted-By:` 줄이 있으면 `True`, 없으면 `False`입니다.

---

## 6. 어떤 지표를 보여주나? (Metrics)

모든 지표의 **기본 단위는 팀(또는 저장소)**, **기본 기간은 최근 12주 트렌드**, **분해축은 AI vs No-AI**입니다.

| 지표 | 무엇인가요 | 어떻게 계산하나요 | AI/No-AI 분해 |
|---|---|---|---|
| **이슈 처리 시간(Lead time)** | 이슈가 작업 시작에서 완료까지 걸린 시간 | `완료 시각 − 처음 "In Progress" 진입 시각`의 중간값(P50) | 이슈에 연결된 커밋 중 AI 커밋 비율로 "AI 중심 / 혼합 / 비AI"로 묶음 |
| **처리량(Throughput)** | 한 주에 끝낸 이슈 수, 스토리포인트 합 | 주별 집계 | 이슈가 속한 묶음 기준 |
| **커밋량** | 한 주에 만든 커밋 수, 추가/삭제된 코드 줄 수 | `commits` 테이블 집계 | `ai_flag = True` vs `False` 직접 비교 |
| **PR 처리 시간** | PR이 열려서 머지되기까지 시간 | `merged_at − opened_at`의 중간값 | PR 첫 커밋의 ai_flag |
| **결함률** | 코드 머지 후 7일/30일 안에 같은 영역에 버그/핫픽스가 또 생긴 비율 | 후속 이슈 추적 | 원 PR/커밋의 ai_flag |
| **Claude 채택률** | 전체 커밋 중 AI 커밋 비중, 활성 사용자 수 | `ai_flag = True / 전체`, 주간 추세 | 본 지표 자체가 분해축 |

### 함께 보여줄 안내문 (대시보드 상단 고정)

> 이 지표들은 **평가가 아니라 진단** 도구입니다.
> 어떤 지표도 단독으로 봐서는 안 됩니다. 예: 처리 시간이 줄었어도 결함률이 함께 올랐다면 진짜 이득이 아닙니다.

---

## 7. 무엇이 좋아지고 무엇이 어려워지나? (Consequences)

**쉬워지는 일**
- Claude 도입 전/후의 같은 팀을 데이터로 비교할 수 있음.
- 라이선스 확대/축소 논의 시 근거 마련.
- 어떤 작업 유형에서 효과가 큰지 보임.

**어려워지는 일**
- Trailer를 붙이는 자동화가 깨지면 데이터가 빔. → 누락률 자체를 대시보드에 띄워 신뢰도를 함께 보여줍니다.
- Squash merge로 trailer가 합쳐지는 경우의 정책 합의 필요.
- "결함률" 정의는 팀마다 다를 수 있어 합의가 필요.

**다시 봐야 할 시점**
- 팀이 50명을 넘으면 SQLite → PostgreSQL.
- 매니저 외 개인 대시보드 요구가 생기면 **별도 ADR**(거버넌스/프라이버시)로 분리.
- 1년 후 지표가 의도대로 행동에 영향을 주는지 회고.

---

## 8. 1주 PoC 일정 (Action Plan)

| 일자 | 할 일 |
|---|---|
| **D1 (수)** | SQLite 스키마 작성, Python 프로젝트 부트스트랩, Jira/Git 토큰 발급 |
| **D2 (목)** | `etl.py`의 Jira 부분: 최근 90일 이슈·상태변경 적재 |
| **D3 (금)** | `etl.py`의 Git 부분: 시범 저장소 1개의 커밋·PR 적재, trailer 파서 테스트 |
| **D4 (월)** | 1시간 스케줄 연결(cron 또는 APScheduler), 실패 시 Slack 알림 1줄 |
| **D5 (화)** | `dashboard.py`(Streamlit) 차트 6개 + 팀/기간 필터 |
| **D6 (수)** | 매니저 5명과 비공식 리뷰 → 라벨/축/필터 조정 |
| **D7 (목)** | 데모, 후속 ADR 항목 식별 (개인 뷰 거버넌스, 결함률 정의 등) |

### PoC 완료 기준 (Definition of Done)

- [ ] 저장소 1개 + Jira 프로젝트 1개가 1시간 주기로 동기화됨
- [ ] 최근 12주 AI/No-AI 커밋 비율 차트가 보임
- [ ] Lead time이 AI/No-AI로 분리되어 보임
- [ ] Trailer 누락률(데이터 품질 지표)이 함께 표시됨

---

## 9. 조심해야 할 것들 (Risks)

- **Goodhart의 법칙** — "AI 커밋 수"를 평가 지표로 삼으면 의미 없는 커밋이 늘어납니다. 대시보드에 *진단용 지표*임을 명시합니다.
- **개인 줄세우기 위험** — PoC에서는 개인 차원 노출 금지. 거버넌스 ADR 후에만 활성화.
- **Squash merge로 trailer 소실** — GitHub 설정에서 "PR 본문을 커밋 본문에 포함"을 켜고, PR 템플릿에 trailer를 자동 포함합니다.
- **자가 보고 편향** — 100% 자동화는 어렵습니다. 누락률 자체를 신뢰도 지표로 표시합니다.

---

## 10. 액션 아이템 (체크리스트)

1. [ ] `prepare-commit-msg` 훅 표준 스크립트 배포 (코드 예시는 동봉된 `prepare-commit-msg` 파일)
2. [ ] PR 템플릿에 "AI 사용 여부 / 강도" 체크박스 추가
3. [ ] Jira `Bug` 이슈 type / `hotfix` 라벨로 "결함률" 산식 합의
4. [ ] `schema.sql`로 SQLite DB 생성
5. [ ] `etl.py` 1시간 스케줄(cron 또는 APScheduler) 연결
6. [ ] `dashboard.py` Streamlit 실행 (`streamlit run dashboard.py`)
7. [ ] 1주 후 데모, ADR-002 초안(개인 뷰 거버넌스) 시작

---

## 11. 동봉된 PoC 코드

| 파일 | 설명 |
|---|---|
| `prepare-commit-msg` | 커밋할 때 `Assisted-By: claude`를 자동으로 붙여주는 Git 훅(Python) |
| `etl.py` | Jira/Git에서 데이터를 가져와 SQLite에 적재하는 1시간짜리 ETL |
| `dashboard.py` | Streamlit 대시보드. 차트 6개와 필터 포함 |
| `schema.sql` | SQLite 테이블 정의 |
| `requirements.txt` | 필요한 Python 패키지 |
| `README.md` | 처음부터 끝까지 실행 가이드 |

---

*본 ADR은 1차 PoC 범위(팀 단위·1시간 주기·1주 일정)만 다룹니다. 개인 단위 측정, 실시간 처리, Cursor 같은 다른 도구 확장은 별도 ADR로 분리합니다.*
